# Allow packages to run lifecycle scripts (postinstall, etc.)
# This suppresses the "Ignored build scripts" warning in pnpm 10+
# Note: pnpm-workspace.yaml takes precedence, this is for backup
onlyBuiltDependencies=@prisma/client @prisma/engines prisma sharp unrs-resolver
